# Feature Modules

Each feature keeps its UI orchestration, validation schemas, use cases and tests together. Database access belongs in `src/server/repositories`, while provider-specific code belongs in `src/server/integrations`.

Planned modules include activities, announcements, authentication, audit, bulletins, contact, giving, ministries, prayer, publishing, sermons and staff management.
