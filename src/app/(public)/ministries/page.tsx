import type { Metadata } from "next";

export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Ministries" };

export { default } from "@/frontend/screens/public/ministries/index";
