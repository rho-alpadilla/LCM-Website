import type { Metadata } from "next";

export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Visitor inquiry" };

export { default } from "@/frontend/screens/admin/inquiries/detail";
